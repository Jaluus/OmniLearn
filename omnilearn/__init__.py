"""OmniLearn core package.

This package is intentionally lightweight and does not require installation
(no `pyproject.toml`). CLI scripts in `OmniLearn/scripts/` add the project root
to `sys.path` via `_bootstrap.py` so imports work from any working directory.
"""

